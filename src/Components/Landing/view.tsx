import React from "react";
import styled from "styled-components";

const Container = styled.section`
position: relative;
  min-height: 100vh;
  z-index:20;
  background-color: #f7f9fc;
`;
const Landing = () => {
  return <Container>Landingasas</Container>;
};

export default Landing;
